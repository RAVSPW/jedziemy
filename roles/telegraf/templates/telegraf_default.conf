[global_tags]
  foo=boo
  default-default

[agent]
  interval = "10s"
  hostname = "{{telegraf_hostname}}"

# OUTPUTS
[[outputs.influxdb]]
  url = "{{telegraf_influx_database}}"
  database = "{{telegraf_influx_database}}"

# INPUTS
[[inputs.cpu]]
  percpu = true
  totalcpu = false
  fielddrop = ["time_*"]